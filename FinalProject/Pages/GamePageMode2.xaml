<Page
    x:Class="FinalProject.Pages.GamePageMode2"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FinalProject.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"  Loaded="Page_Loaded">
    <Canvas x:Name="arena" Background="LightBlue">
        <Viewbox>
            <Button x:Name="backButton" BorderThickness="2" BorderBrush="White" CornerRadius="20" Margin="5" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Content="back" FontSize="20" Click="BackButton_Click"></Button>
        </Viewbox>
        <Viewbox Canvas.Left="700">
            <TextBlock VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Text="Score: " FontSize="40" ></TextBlock>
        </Viewbox>
        <Viewbox Canvas.Left="820">
            <TextBlock x:Name="score" Margin="5" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Text="0" FontSize="40"></TextBlock>
        </Viewbox>
    </Canvas>
</Page>
